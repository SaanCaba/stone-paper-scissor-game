import React from 'react'
import styles from '../../styles/spinner.module.css'

function Loader() {
  return (
    <div className={`${styles.spinner} max-sm:h-24 max-sm:w-24`} >

    </div>
  )
}

export default Loader